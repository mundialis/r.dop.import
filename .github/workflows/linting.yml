name: Linting and code quality check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    uses: mundialis/github-workflows/.github/workflows/linting.yml@md_lint
    with:
      VALIDATE_HTML: false
